import React from 'react'

const App = props => {
    
  return  (<div>
      <h1>Welcome to Routing</h1>
        <p>This demo branch contains some missing components and existing components. In this demo we will build the following:</p>
        <ul>
            <li>Create routes for existing pages/components.</li>
            <li>Create buzzwords list and show pages</li>
            <li>Build a very simple component that uses the <code>withRouter()</code> Higher Order Component (HOC).</li>
        </ul>
</div>)

}

export default App

